events {}

http {
    upstream blue {
        server host.docker.internal:8080;
    }

    upstream green {
        server host.docker.internal:8081;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://green;
        }
    }
}